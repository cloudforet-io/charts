apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: console
spec:
  ingressClassName: {ingress_class_name}
  tls:
  - hosts:
    - "*.{your_domain}"
    secretName: {tls_secret_name}
  rules:
  - host: "*.{your_domain}"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: console
            port:
              number: 80
